export class Assets {
     private name: string;
     private address: string;
     private c_assetId: string;

     constructor(name: string, address: string, c_assetId: string) {
          this.name = name;
          this.address = address;
          this.c_assetId = c_assetId;
     }
}
